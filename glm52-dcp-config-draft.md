# [DRAFT] GLM-5.2 等大模型的 DCP 配置策略

> **⚠️ DRAFT 状态**：本文为草稿，内容基于推测和理论分析，部分结论需要实测验证。

## 核心问题

**GLM-5.2 最大尺寸能否开 DCP 2/4/8？**

答案：**取决于模型的 KV 头数和 TP 并行度**。

---

## 前提：理解 DCP 上限公式

```
DCP 上限 = TP 并行度 ÷ KV 头数
```

要开启 DCP=8，必须满足：
1. **TP ÷ KV头数 ≥ 8**（有足够的复制倍数）
2. **TP 能被 8 整除**（整除约束）

---

## GLM 系列模型的 KV 头数

根据公开信息：

| 模型 | KV 头数 | 数据来源 |
|---|---|---|
| GLM-4-9B | **2** | 官方配置 |
| GLM-5.1-14B | **8** | 官方配置 |
| GLM-5.2-14B | **8（推测）** | 参考 GLM-5.1 |
| GLM-5.2-27B | **8-16（推测）** | 需实测确认 |
| GLM-5.2（最大版本） | **8-32（推测）** | 需实测确认 |

**确认方法**：
```bash
grep num_key_value_heads /path/to/model/config.json
```

---

## 场景分析：不同 TP 下能开的 DCP

### 场景1：GLM-5.2 有 **8 个 KV 头**（参考 GLM-5.1-14B）

| TP 并行度 | 硬件配置示例 | 复制倍数 | DCP 上限 | 能开 DCP=2? | 能开 DCP=4? | 能开 DCP=8? |
|---|---|---|---|---|---|---|
| **TP8** | 1机×8卡 | 8÷8 = **1×** | 1 | ❌ | ❌ | ❌ |
| **TP16** | 2机×8卡 | 16÷8 = **2×** | 2 | ✅ | ❌ | ❌ |
| **TP32** | 4机×8卡 | 32÷8 = **4×** | 4 | ✅ | ✅ | ❌ |
| **TP64** | 8机×8卡 | 64÷8 = **8×** | 8 | ✅ | ✅ | ✅ |

**关键结论**：
- **TP8**：无重复，**不需要开 DCP**（已最优）
- **TP16**：只能开 DCP=2
- **TP32**：能开 DCP=2, 4
- **TP64**：能开 DCP=2, 4, 8 ✅

### 场景2：GLM-5.2 有 **16 个 KV 头**（更大模型可能配置）

| TP 并行度 | 硬件配置示例 | 复制倍数 | DCP 上限 | 能开 DCP=2? | 能开 DCP=4? | 能开 DCP=8? |
|---|---|---|---|---|---|---|
| **TP16** | 2机×8卡 | 16÷16 = **1×** | 1 | ❌ | ❌ | ❌ |
| **TP32** | 4机×8卡 | 32÷16 = **2×** | 2 | ✅ | ❌ | ❌ |
| **TP64** | 8机×8卡 | 64÷16 = **4×** | 4 | ✅ | ✅ | ❌ |
| **TP128** | 16机×8卡 | 128÷16 = **8×** | 8 | ✅ | ✅ | ✅ |

**关键结论**：
- **TP16**：无重复，**不需要开 DCP**
- **TP32**：只能开 DCP=2
- **TP64**：能开 DCP=2, 4
- **TP128**：能开 DCP=2, 4, 8 ✅

### 场景3：GLM-5.2 有 **32 个 KV 头**（最大模型的极端情况）

| TP 并行度 | 复制倍数 | DCP 上限 | 能开 DCP=8? |
|---|---|---|---|
| TP32 | 32÷32 = 1× | 1 | ❌ 无重复，不需要 |
| TP64 | 64÷32 = 2× | 2 | ❌ 上限仅 2 |
| TP128 | 128÷32 = 4× | 4 | ❌ 上限仅 4 |
| **TP256** | 256÷32 = 8× | 8 | ✅ **能开** |

---

## 硬件选择：MI300X vs B200

| 硬件 | 显存 | 优势 | 与 DCP 的关系 |
|---|---|---|---|
| **MI300X** | 192GB HBM3 | 显存大，适合长上下文 | ✅ 显存大 → 可能不需要 DCP |
| **B200** | ~200GB（预计） | NVIDIA 生态成熟 | 与 DCP 无直接关系 |

**关键点**：
- **DCP 只与 TP 并行度和 KV 头数有关，与具体硬件型号无关**
- MI300X 显存大（192GB）→ 单卡能装更多 KV Cache → **可能降低对 DCP 的需求**

---

## 判断流程

### 步骤1：确认模型配置
```bash
# 查看 KV 头数
grep num_key_value_heads /path/to/glm-5.2/config.json
```

### 步骤2：计算复制倍数
```
复制倍数 = 你的 TP 并行度 ÷ KV 头数
```

### 步骤3：判断是否需要开 DCP
- **复制倍数 = 1**：✅ 无重复，**不需要开 DCP**
- **复制倍数 ≥ 2**：考虑开 DCP

### 步骤4：计算 DCP 上限
```
DCP 上限 = TP 并行度 ÷ KV 头数
```

### 步骤5：选择 DCP 值
- DCP 必须满足：`TP 能被 DCP 整除`
- DCP 不能超过上限

---

## 实际案例

### 案例1：GLM-5.2（8个KV头）+ 8机×8卡 MI300X（TP64）

```bash
# 1. 确认配置
KV头数 = 8
TP = 64

# 2. 计算
复制倍数 = 64 ÷ 8 = 8×
DCP上限 = 8

# 3. 判断
✅ 有重复存储，需要开 DCP
✅ 能开 DCP=2（64÷2=32✓）
✅ 能开 DCP=4（64÷4=16✓）
✅ 能开 DCP=8（64÷8=8✓）
```

**推荐配置**：
```bash
vllm serve glm-5.2 \
  --tensor-parallel-size 64 \
  --decode-context-parallel-size 8  # 最大化消除重复
```

### 案例2：GLM-5.2（8个KV头）+ 2机×8卡 MI300X（TP16）

```bash
# 1. 确认配置
KV头数 = 8
TP = 16

# 2. 计算
复制倍数 = 16 ÷ 8 = 2×
DCP上限 = 2

# 3. 判断
✅ 有重复存储，需要开 DCP
✅ 能开 DCP=2（16÷2=8✓）
❌ 不能开 DCP=4（上限仅2）
❌ 不能开 DCP=8（上限仅2）
```

**推荐配置**：
```bash
vllm serve glm-5.2 \
  --tensor-parallel-size 16 \
  --decode-context-parallel-size 2  # 上限只有2
```

### 案例3：GLM-5.2（8个KV头）+ 1机×8卡 MI300X（TP8）

```bash
# 1. 确认配置
KV头数 = 8
TP = 8

# 2. 计算
复制倍数 = 8 ÷ 8 = 1×

# 3. 判断
❌ 无重复，不需要开 DCP
```

**推荐配置**：
```bash
vllm serve glm-5.2 \
  --tensor-parallel-size 8
  # 不需要开 --decode-context-parallel-size
```

---

## 关键规律总结

### 规律1：要开 DCP=8 的硬性要求

| 模型 KV 头数 | 最小 TP 要求 | 硬件配置示例 |
|---|---|---|
| 8 个 | **TP64** | 8机×8卡 |
| 16 个 | **TP128** | 16机×8卡 |
| 32 个 | **TP256** | 32机×8卡 |

**结论**：开 DCP=8 需要**非常大的部署规模**。

### 规律2：KV 头数越多，越不需要 DCP

- **GLM-4-9B（2个KV头）**：TP4 就开始有 2× 重复
- **GLM-5.1-14B（8个KV头）**：TP8 无重复，TP16 才开始有 2× 重复
- **假设有 32个KV头**：TP32 无重复，TP64 才开始有 2× 重复

**结论**：大模型通常有更多 KV 头 → 不容易产生重复 → **DCP 需求降低**。

### 规律3：MI300X 显存大，可能不需要 DCP

**显存对比**：
- A100 80GB：长上下文时显存紧张 → **DCP 收益明显**
- MI300X 192GB：显存充裕 → DCP 的显存翻倍收益**不那么紧迫**

**但 DCP 的性能收益仍然有效**：
- 长上下文（≥128K）+ 低并发（≤2）：TPOT 提升 45%
- 这部分收益与显存大小无关

---

## 开启建议决策树

```
1. 你的 TP 并行度是多少？
   └─ TP8
       └─ 检查：TP ÷ KV头数 = ?
           └─ = 1 → ❌ 不需要 DCP
           └─ ≥ 2 → ✅ 可以开 DCP=2

   └─ TP16
       └─ 检查：TP ÷ KV头数 = ?
           └─ = 1 → ❌ 不需要 DCP
           └─ = 2 → ✅ 可以开 DCP=2
           └─ ≥ 4 → ✅ 可以开 DCP=2, 4

   └─ TP32
       └─ 检查：TP ÷ KV头数 = ?
           └─ = 1 → ❌ 不需要 DCP
           └─ = 2 → ✅ 可以开 DCP=2
           └─ = 4 → ✅ 可以开 DCP=2, 4
           └─ ≥ 8 → ✅ 可以开 DCP=2, 4, 8

   └─ TP64+
       └─ 检查：TP ÷ KV头数 = ?
           └─ 按上述规律继续

2. 你的场景是什么？
   └─ 显存不足 → ✅ 必须开 DCP（翻倍容量）
   └─ 长上下文（≥128K）+ 低并发（≤2） → ✅ 建议开（TPOT 提升 45%）
   └─ 短上下文 + 高并发（≥4） → ❌ 不建议（TTFT 代价大）
```

---

## 待确认问题

1. **GLM-5.2 最大版本的 KV 头数是多少？**
   - 需要实测确认 `config.json`

2. **MI300X 的带宽特性如何影响 DCP 性能？**
   - 微信文章实测基于昇腾 910B4
   - MI300X 的 Infinity Fabric 带宽可能不同

3. **B200 的 NVLink 5.0 对 DCP 有何影响？**
   - NVLink 5.0 带宽 1.8TB/s（vs NVLink 4.0 的 900GB/s）
   - 更高带宽可能降低 DCP 的 TTFT 代价

---

## 下一步

**如果你能提供以下信息，我可以给出精确建议**：

1. **模型配置**：
   ```bash
   cat /path/to/glm-5.2/config.json | grep num_key_value_heads
   ```

2. **部署规模**：
   - 单机还是多机？
   - 每机多少卡？
   - TP 并行度是多少？

3. **应用场景**：
   - 上下文长度范围？
   - 预期并发度？
   - 主要卡 TTFT 还是 TPOT？

---

## 参考资料

[1] DCP 实测:消除张量并行下的 KV Cache 重复存储,收益、代价与适用边界. 微信公众号. https://mp.weixin.qq.com/s/0M-_6lE6z87Sk67FaDrgkA

[2] 本站文章：[DCP 实测：消除张量并行下的 KV Cache 重复存储](./dynamic-context-parallelism.html)

---

**最后更新**：2026-08-12

**状态**：DRAFT - 需要实测验证
